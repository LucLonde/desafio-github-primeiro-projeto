#include<stdio.h>

int main(){

    int i, x = 0, vet[50];
    
    for ( i = 0; i < 50; i++)
    {
        (x + 5 * x) % (x + 1);
    }
    for ( i = 0; i < 50; i++)
    {
        printf("o valor: %d\n", vet[i]);
    }
    

    return 0;
}